interface:
  display_name: "Loopex"
  short_description: "Build and review with a user checkpoint every round"
  default_prompt: "Use $loopex for my task with my chosen reviewer model, optional reasoning effort, and maximum review rounds."
policy:
  allow_implicit_invocation: false
