# Consumer retirement evidence

Recorded 2026-09-24 on Windows 11, Node 24.15.0, npm 11.12.1 and Chrome 153.
NGNE parent 9401adb3474d44890d7a2b892beda0cd9b5591d7 with uncommitted changes.
This record supplements earlier evidence; it does not rewrite its original status.

The [machine record](consumer-retirement.json) enumerates all 96 original browser
assertion sites, including dynamic labels and shared assertion helpers. They are
preserved in consumer revision c04cf1d013a57773a59a601ff0994f5e6bee1864 and their
emitted observations passed at both URL bases. The consumer is clean, with no new
commit or push. Its existing acceptance and separate full measurements retain
package identities, budgets, diagnostics and cleanup.

All 37 mixed engine assertions have exact named replacement observations or tests,
including the 21 previously unresolved correspondences. Animation playback belongs
to consumer src/animation.ts; its independent-clock test and original browser
assertion passed. This corrects ownership, rather than inventing an engine API.
The minimal engine fixture uses smaller scene asset sets; authored eight-claim,
three-image/four-consumer counts remain checked unchanged by consumer acceptance.

Engine browser additions check distinct external replacement/return pixels,
stop invalidation and fresh retry, retained audio/simulation, focus release during
simulation, surviving scene execution after failed preparation, and transition
publication during recovery. Expected stale-candidate diagnostics are asserted
and retained separately. Existing preparation/cancellation/recovery/disposal tests
remain active. The asset-restore regression retains a real failing first restore,
successful second restore, attempted script removal and original workload error
through the shared cleanup primitive.

Superseded consumer runners, assertions, game snapshot and active policy/environment
configuration were removed after passing replacement coverage. Historical procedures
and configuration bytes are retained under [retired-content](retired-content/README.md).
Ordinary CI now requires engine-only installed root/nested validation and retains
all three browser failure injections and the showcase deployment dependency.
The frozen baseline workflow and original baseline identities remain unchanged.
Legacy strict/advisory comparators and their independent retirement gates remain.

Final consumer-independent checks passed:

- 262 tests; build/browser build, tooling types, formatting, migration and exact package handoff.
- Two standalone installed runs: 60 observations at each root/nested base, successful cleanup.
- Two ordinary browser runs: 189 checks each; all three failure injections and real profiler transport passed their expected outcomes.
- Two full engine resource runs: 360 transitions, nine cancellations and three disposals each.
- Fresh pinned consumer conformance against the newly prepared package; consumer still clean.

The [raw archive](consumer-retirement-raw.json.gz) retains 426 files, original run
inventories, logs, observations, packages, selected source and development failures.
Every inventoried payload hash/size and archive round trip was verified. Disposable
installations/builds and dependency trees remain local and are omitted. The machine
record identifies each execution and its package, source and evidence identities.

Software WebGPU browser acceptance, headless non-fallback resource measurements,
and manual visual/audio acceptance remain distinct. No fresh Linux/hosted execution,
controlled performance comparison or final CI cost acceptance is claimed.
